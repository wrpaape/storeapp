<% @cart_items.each.with_index do |cart_item, ind| %>
  <%= react_component("CartItem", { name: cart_item.product.name, quantity: cart_item.quantity, price_per: cart_item.product.price, cart_item_id: cart_item.id, user: @user }) %>
<% end %>
